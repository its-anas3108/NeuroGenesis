# Stale artifacts — not current results

The PNG files in this directory were produced by a run on a different machine
before the Stage-Aware refactor. They are retained only as historical
reference and are **not** outputs of the current framework.

- Nothing in the active codebase reads this directory.
- All current artifacts are written under `outputs/` (see `modules/common/paths.py`).
- Do not cite figures from this directory as results.
